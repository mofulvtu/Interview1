package com.java.sort;

/**
 * 希尔排序
 * 
 * 基本思想：
 * 算法先将要排序的一组数按某个增量d(n/2,n为要排序的个数)分成若干组，每组中记录的
 * 下表相差d，对每组中全部元素进行直接插入排序，然后再用一个较小的增量(d/2)对它
 * 进行分组，在每组中再进行直接插入排序。当增量减到1时，进行直接插入排序后，排序完成。
 * @author 刘增刚
 *
 */
public class ShellSort {
	

}
